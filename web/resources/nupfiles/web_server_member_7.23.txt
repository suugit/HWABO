
보이게 안보이게를 css에서 처리

로그인 제한 때문에 SQL devleoper 이용 컬럼 추가

ALTER TABLE MEMBER
ADD (LOGIN_OK CHAR(1) DEFAULT 'Y');

ALTER TABLE MEMBER
ADD CONSTRAINT CHK_MEMB_LOK CHECK (LOGIN_OK IN ('Y', 'N'));

COMMIT;

onchange="changeLogin(this);" 체크 상태가 변경되었을 때 
 > 함수명에 this는 지금 태그 자신을 의미(radio)를 의미


모든 브라우저는 개발자 도구를 제공하고 있다 .
F12를 누르면 뜬다.
자바 스크립트는 오류가 났는지 확인할 방법이 없다.
에러가 나도 프론트단에 보일 것은 보임. 
그래서 개발자 도구를 이용해서 오류잡음.


subString(8) 이거 글자수 셀때 0부터 시작.
저렇게 쓰면 8부터 끝까지라는 의미

에이작스 ?  

jsp에서 자바 서블릿 호출하는 것 : location.href 
		저거 안에 쿼리스트링

DB설계 손댈 것이 너무 많아지므로 설계 꼼꼼히
휴학여부 휴학카운트?(3번까지만) 복학

스트링은 equals 

컨트롤러가 DB랑 값을 이어줘야한다. 값변환 해주는 역할

update한 다음에 list조회를 다시해야한다. (에이작스면 이러한 과정이 필요없음)
서블릿으로 하면 이러한 불편함있다.

겟 어트리뷰트(name)는 오브젝트
set 어트리뷰트(name, value)



라디오의 특징 : 하나의 값으로 이름 같게 하면 하나의 값만 입력가능


<fieldset id="ss">   > 필드를 만들어주는 것

<legend></legend> 필드안에 제목을 만들고 싶을 때

</fieldset>


css로 legend 사이즈 조절


쿼리스트링은 다른 값을 서블릿으로 보낼 수 있는데 
form태그는 다른 값을 어떻게 보낼 것이냐
화면에는 안보이지만 전송은 가능. 이 사람이 고른 것이 아이디이다 라는걸 알려줄 표시가 될 것임



전체회원관리페이지에서 항목을 고르면 그 항목만 보이게 할 건데
이거는 jquery이용

자바스크립트를 이용해서 짜놓은 코드를 모아서 제공하는 게 제이쿼리닷컴
제이쿼리 : 제이쿼리사가 만들어 놓은 자바스크립트 오픈소스

원하는 태그를 선택하는 방법은 3가지뿐

jquery.com 에서 다운 

Download the compressed, production jQuery 3.5.1 (compressed 다운받아서 쓰면됨. 웹에서 쓰려면 가벼운 것이 좋다)
Download the uncompressed, development jQuery 3.5.1 (압축안된 것. 용량 10배 큼)

크롬다운로드 오류 : 우클릭 다른이름으로링크저장 
다운받은 것 web > resources > js 에 붙여넣기

사용할 때엔 
https://api.jquery.com/

여기서 도움말 보면서 사용해야한다.
<script type="text/javascript" src="/test1/resources/js/jquery-3.5.1.min.js"> </script>

이렇게 사용할 jsp파일에서 써주어야한다.

선택의 형태는 배열이다. 자동으로 배열으로 ?

무언가를 선택한다의 의미 $("") 
	"  " 안의 값을 선택한다

$("input[name=item]").on("change", function(){});
 => 이렇게 쓰면 각각 onchange 써준 것과 같은 효과
 => 저 뒤의 function 은 콜백함수.
 => 이벤트가 발생하면 자동으로 콜백함수 작동


로그인 페이지 > 로그인 버튼누르면 로그인 서블릿 아이디패스워드 전달받아서 서비스로 전달 > 서비스에선 dao > 로그인 성공

로그아웃 > 로그아웃 서블릿 > 세션처리 



시험에 나오는 것 표시한 페이지
3 4 28 30 31 33 46 48 


서블릿의 /sddsd는 url 맵핑에 사용

멤버컨트롤러 new 서블릿 doGet에 작성

// 뷰로 내보내는 값에 한글이 있다면 인코딩해야함
response.setContentType("text/html; charset=utf-8");  >> 이 안의 값은 view의 page태그안의 contentType 써주는 것이다.















